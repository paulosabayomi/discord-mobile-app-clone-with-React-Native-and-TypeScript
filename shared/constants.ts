import React from "react";
import { IChannelListContext } from "./types";
export const app_storage_key = "appColorModeKey"
export const ChannelListContext = React.createContext({} as IChannelListContext)